<!DOCTYPE html>
<html>
  <head>
    <title>Cabler</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= turbo_stream_from :onliners, channel: OnlineChannel %>
    Current website visitors: <span id='online-counter'></span>

    <%= yield %>
  </body>
</html>
